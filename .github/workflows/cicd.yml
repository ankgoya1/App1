name: CI/CD Workflow

on:
  push:
    branches:
      - main

env:
  MY_VAR: Testing
  USER: ${{ secrets.USER }}
  
jobs:
  Checkout:
    runs-on: ubuntu-latest
    outputs:
      my_var: ${{ env.MY_VAR }}
      user: ${{ secrets.USER }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

  Build:
    needs: Checkout
    uses: ankgoya1/pr/.github/workflows/build.yml@main

  Publish:
    needs: Build
    uses: ankgoya1/pr/.github/workflows/publish.yml@main
    with:
      MY_VAR: ${{ needs.Build.outputs.my_var }}
      USER: ${{ needs.Build.outputs.user }}
